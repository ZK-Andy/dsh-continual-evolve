[中文](#cn-v0.10.2) | [English](#en-v0.10.2)

<h3 id="cn-v0.10.2">新功能（记录语言跟随客户端）</h3>
- 自进化记录跟随 DSH 客户端语言：决议链为显式 `recordLanguage` 配置 → 持久化 `locale.preference`（复用上游键，不自创）→ 轨迹中文信号 → `en` 兜底；review/planner/memory-agent/wrap-up 四处 prompt 追加语言指令（JSON 键/id/path/代码不动）；四个确认弹窗升级为 zh/en 双语字典，解析器双语接受；持久化读取 fail-open (@zhangkun) <!-- b304dd7 -->

### 问题修复
- （无）

### 其他改动
- （无）

<h3 id="en-v0.10.2">Features (records follow the client language)</h3>
- Evolution records now follow the DSH client language: explicit `recordLanguage` config → durable `locale.preference` (upstream keys reused) → trajectory Chinese signal → `en` fallback; all four record-authoring prompts carry a language instruction (JSON keys/ids/paths/code untouched); the four confirmation dialogs become a zh/en dictionary with bilingual parser acceptance; the durable read is fail-open (@zhangkun) <!-- b304dd7 -->

### Bug Fixes
- (none)

### Chores
- (none)

Full Changelog: [v0.10.1...v0.10.2](https://github.com/ZK-Andy/dsh-continual-evolve/compare/v0.10.1...v0.10.2)
npm: https://www.npmjs.com/package/dsh-continual-evolve/v/0.10.2
