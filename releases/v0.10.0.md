[中文](#cn-v0.10.0) | [English](#en-v0.10.0)

<h3 id="cn-v0.10.0">新功能（记忆提案口径收紧 + 拒绝回路）</h3>
- `/evolve` 目录行中英双语：catalog 描述改为中文在前、英文在后，`/` 面板中文 locale 下直接可读；命令解析不受影响（仍只认英文名）(@zhangkun) <!-- 6a45c4c -->
- 提案口径收紧：已应用且闭环、无复发证据的一次性排障结论不再提案（落在盘上的修复状态本身就是记录）；偏好、项目规则、可复用教训标准不变 (@zhangkun) <!-- be6b6b5 -->
- 被拒提案跨会话去重：project/global 整批按内容指纹记入 `evolve/declined-memory.json`（50 条上限），同一批再出现直接 suppressed、不再弹窗，审计留标记；改述复活仍会询问 (@zhangkun) <!-- 58daede -->
- 触发前预检：checkpoint 覆盖已拒内容（≥85% 且 ≥5 token）时连 Memory Agent 都不唤起，单次省约 1,900 token；被拒内容进台账前先过密钥筛查，密钥形只成立、不持久化 (@zhangkun) <!-- 46e3b0f -->

### 问题修复
- （无）

### 其他改动
- （无）

<h3 id="en-v0.10.0">Features (memory proposal bar + decline loop)</h3>
- Bilingual `/evolve` catalog row: Chinese-first description in `/` menus under Chinese locales; command parsing unchanged (English name only) (@zhangkun) <!-- 6a45c4c -->
- Tighter proposal bar: already-applied, closed troubleshooting with no recurrence evidence is no longer proposed (the fixed state on disk is its own record); preferences, project rules, and reusable lessons unaffected (@zhangkun) <!-- be6b6b5 -->
- Cross-session dedup of declined proposals: declined project/global batches recorded by content fingerprint in `evolve/declined-memory.json` (cap 50); exact repeats suppressed without another popup, audit-marked; rephrased batches still ask (@zhangkun) <!-- 58daede -->
- Pre-agent precheck: checkpoints overlapping declined content (≥85% coverage, ≥5 tokens) skip the Memory Agent entirely (~1,900 tokens saved per hit); declined content is secret-screened before entering the ledger (@zhangkun) <!-- 46e3b0f -->

### Bug Fixes
- (none)

### Chores
- (none)

Full Changelog: [v0.9.0...v0.10.0](https://github.com/ZK-Andy/dsh-continual-evolve/compare/v0.9.0...v0.10.0)
npm: https://www.npmjs.com/package/dsh-continual-evolve/v/0.10.0
